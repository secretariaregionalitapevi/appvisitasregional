{
  "name": "app-visitas-regional-itapevi",
  "version": "1.0.0",
  "scripts": {
    "build": "python3 -m pip install -r requirements.txt && python3 manage.py collectstatic --noinput && cp -R ColorAdminApp/static/. static/"
  }
}
